on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: "Testing"
        run: ls -a
      - uses: actions/checkout@v3
      - name: "Testing after"
        run: ls -a